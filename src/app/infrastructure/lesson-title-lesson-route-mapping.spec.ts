import { LessonTitleLessonRouteMapping } from './lesson-title-lesson-route-mapping';

describe('LessonTitleLessonRouteMapping', () => {
  it('should create an instance', () => {
    expect(new LessonTitleLessonRouteMapping()).toBeTruthy();
  });
});
